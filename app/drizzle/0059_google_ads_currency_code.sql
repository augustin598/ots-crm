-- Add currency_code column to google_ads_account
-- This column was already added manually to the database, so this is a no-op to sync drizzle state.
-- If running on a fresh database, the column is included in the CREATE TABLE from earlier migrations.
SELECT 1;
