SQLITE_PATH='local.db'
MINIO_ENDPOINT=s3.navitech.cloud
MINIO_ACCESS_KEY=BeT1hPlTntKyzMIAwvaC
MINIO_SECRET_KEY=KGKrUtivteBKzMPeNrUPDAz1KYfXv7bM9RfCZhUz
MINIO_BUCKET_NAME=navitech
MINIO_USE_SSL=true
MINIO_PORT=443

# SMTP (email) - folosit ca fallback global când tenant-ul nu are setări proprii
SMTP_HOST=sender.navitech.cloud
SMTP_PORT=2525
SMTP_USER=notify@app.navitech.cloud
SMTP_PASSWORD=your-smtp-password
SMTP_FROM=notify@app.navitech.cloud
# Originea publică a aplicației — baza pentru TOATE link-urile trimise prin email
# (task-uri, invitații, magic link portal client, semnare contracte).
# OBLIGATORIU și în development: baza de date de dev e cea de PRODUCȚIE, deci un
# email declanșat în timp ce testezi local pleacă la clienți reali. Nesetat →
# link-uri http://localhost:5173/... inutilizabile pentru destinatar.
PUBLIC_APP_URL=https://clients.onetopsolution.ro

# Cheie DEDICATA pentru extrasele contabile (Stripe Reporting API).
# Reporting API ruleaza doar pe date LIVE, in timp ce cheia de plati de mai sus
# poate ramane pe test. Recomandat: restricted key rk_live_... cu drepturi
# Report runs (write) + Report types (read) + Files (read).
STRIPE_REPORTING_KEY=
