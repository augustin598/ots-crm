app-config.json
.agents/
.claude/skills/
.claude/worktrees/
skills-lock.json
.DS_Store

# Database backups (taken before risky migrations; never committed)
backups/
whmcs-backup-*.sql

# Local design exploration / mock-ups (not part of the app)
new_design/

# Plugin build artifacts (use ots-wp-connector/build.sh to produce)
ots-wp-connector*.zip
